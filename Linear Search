//Linear search

#include<bits/stdc++.h>
using namespace std;
vector<int> vec;

int main(){

    int n,x;
    cin>> n;

    for(int i=0; i<n; i++){
        cin>> x;
        vec.push_back(x);
    }

    //vec.push_back(100);

    for(int i=0; i<vec.size(); i++){
        cout<< vec[i]<< " ";
    }

    cout<< endl;

    //finding value
    int f;
    cin>> f;

    int flag=0;

    for(int i=0; i<vec.size(); i++){
        if(f==vec[i]){
            flag = 1;
            break;
        }
    }

    if(flag==1){
        cout<< "YES";
    }
    else{
        cout<< "NO";
    }

    return 0;
}
